1번
clear; clc;

x = 0:0.05:6*pi;
y = sin(x) + 0.5*cos(3*x);

plot(x, y)
xticks(0:pi:6*pi)
xticklabels({'0', '\pi', '2\pi', '3\pi', '4\pi', '5\pi', '6\pi'})
title('y = sin(x) + 0.5cos(3x)')
xlabel('x')
ylabel('y')
grid on
2번
clear; clc;

x = 1:1:50;
y = 3.^x;

subplot(1,2,1)
plot(x, y)
title('Linear Plot')
xlabel('x'), ylabel('y')

subplot(1,2,2)
semilogy(x, y)
title('Semilogy Plot')
xlabel('x'), ylabel('y')
일반 plot으로 그리면 y=3^x 값이 뒤로 갈수록 기하급수적으로 너무 커져버려서, 앞부분에서 x가 작을 때의 변화는 거의 선에 딱 붙어서 안 보입니다. 
반면에 y축을 로그 스케일로 바꾼 semilogy를 사용하면 지수 함수 형태가 깔끔한 직선으로 나와서, 값 범위가 아무리 넓어도 전체적으로 어떻게 커지고 있는지 추세를 한눈에 알아보기 좋습니다.
3번
clear; clc;

score = [72, 85, 91, 66, 78, 88, 95, 54, 60, 73, 82, 90, 69, 77, 84, 92, 58, 63, 79, 87];
save('score.dat', 'score', '-ascii');
data = load('score.dat');

subplot(1,2,1)
histogram(data, 5)
title('Histogram (5 Bins)')
xlabel('Score')
ylabel('Frequency')

subplot(1,2,2)
histogram(data, 10)
title('Histogram (10 Bins)')
xlabel('Score')
ylabel('Frequency')
4번
clear; clc;

f = @(x) sin(x).*exp(-0.1*x);

fplot(f, [0, 10*pi])
title('f(x) = sin(x)e^{-0.1x}')
xlabel('x')
ylabel('f(x)')
grid on
5번
clear; clc;

x = 0:0.1:10;
y1 = x.^2;
y2 = exp(0.5*x);

[ax, h1, h2] = plotyy(x, y1, x, y2);
title('Comparison of Polynomial and Exponential Growth')
xlabel('x')
ylabel(ax(1), 'y_1 = x^2')
ylabel(ax(2), 'y_2 = e^{0.5x}')
처음에는 y1=x^2와 y2=e^(0.5x) 둘 다 고만고만하게 커지는 것처럼 보입니다.
그런데 x값이 조금만 커져도 지수함수인 y2가 다항식인 y1보다 압도적으로, 훨씬 더 가파르게 증가하는 것을 볼 수 있습니다.
6번
clear; clc;

t = 0:0.02:12*pi;
x = (1 + 0.03*t).*cos(t);
y = (1 + 0.03*t).*sin(t);
z = 0.2*t;

plot3(x, y, z)
title('Parametric Curve')
xlabel('x(t)')
ylabel('y(t)')
zlabel('z(t)')
grid on
7번
clear; clc;

[X, Y] = meshgrid(-4:0.2:4, -4:0.2:4);
Z = X.^2 - Y.^2;

mesh(X, Y, Z)
title('z = x^2 - y^2')
xlabel('X'), ylabel('Y'), zlabel('Z')
saddle surface 형태가 맞습니다. 중심을 기준으로 x축 방향으로 보면 아래로 볼록한 U자 모양인데, y축 방향을 기준으로 보면 뒤집힌 U자 모양이 나옵니다. 이 두 모양이 겹쳐지다 보니 딱 말안장 같은 입체적인 지형이 만들어집니다.
8번
clear; clc;

[X, Y] = meshgrid(-2*pi:0.1:2*pi, -2*pi:0.1:2*pi);
Z = sin(X).*cos(Y);

subplot(1,2,1)
mesh(X, Y, Z)
title('Mesh Graph')
xlabel('X'), ylabel('Y'), zlabel('Z')

subplot(1,2,2)
contour(X, Y, Z)
title('Contour Graph')
xlabel('X'), ylabel('Y')
contour 그래프는 3D 지형도에서 등고선을 그리는 것과 똑같습니다. 굳이 3차원 그래프를 이리저리 돌려보지 않아도 2차원 평면 위에서 선들의 간격이나 둥글게 모이는 모양만 보고 함수의 높낮이와 지형 정보를 직관적으로 파악할 수 있게 해줍니다.
9번
clear; clc;

t = 0:0.01:25;
t(t==0) = [];

y = exp(-0.15*t).*cos(4*t);
env_up = exp(-0.15*t);
env_down = -exp(-0.15*t);

figure;
plot(t, y, 'b-', t, env_up, 'r--', t, env_down, 'r--')
title('Damped Oscillation with Envelopes')
xlabel('t'), ylabel('y(t)')
legend('y(t)', 'Upper Envelope', 'Lower Envelope')

figure;
semilogy(t, abs(y))
title('Semilogy of |y(t)|')
xlabel('t'), ylabel('|y(t)|')
절대값을 씌운 신호 |y(t)|를 semilogy로 그려보면, 위아래로 진동하면서 줄어들던 폭이 로그 스케일 덕분에 쭉 뻗은 일직선 형태로 내려가는 것을 볼 수 있습니다. 지수적으로 감소하던 게 선형적인 빗면처럼 보이게 돼서, 감쇠되는 정도를 시각적으로 파악하기 훨씬 편합니다.
10번
clear; clc;

[X, Y] = meshgrid(-6:0.1:6, -6:0.1:6);
Z = cos(X).*sin(Y).*exp(-0.05*(X.^2 + Y.^2));

figure;
mesh(X, Y, Z)
title('Mesh Plot')
xlabel('X'), ylabel('Y'), zlabel('Z')

figure;
contour(X, Y, Z)
title('Contour Plot')
xlabel('X'), ylabel('Y')

[max_val, linear_idx] = max(Z, [], 'all');
[row, col] = ind2sub(size(Z), linear_idx);
max_x = X(row, col);
max_y = Y(row, col);

fprintf('최대값: %f\n', max_val);
fprintf('최대값 위치: (x = %f, y = %f)\n', max_x, max_y);
11번
clear; clc;

t = [0, 1, 2, 3, 4, 5, 6];
C = [12, 8.9, 6.6, 4.9, 3.6, 2.7, 2.0];

figure;
plot(t, C, '-o')
title('Linear Plot of C(t)')
xlabel('t'), ylabel('C(t)')

figure;
semilogy(t, C, '-o')
title('Semilogy of C(t)')
xlabel('t'), ylabel('C(t)')

p = polyfit(t, log(C), 1);
k = -p(1);
fprintf('추정된 k의 값: %f\n', k);
12번
clear; clc;

n = 1:1:30;
T1 = n;
T2 = n.*log(n);
T3 = n.^2;
T4 = 2.^n;

figure;
plot(n, T1, n, T2, n, T3, n, T4)
title('Algorithm Time Complexity (Linear)')
xlabel('n'), ylabel('T(n)')
legend('T_1 = n', 'T_2 = n log(n)', 'T_3 = n^2', 'T_4 = 2^n', 'Location', 'best')

figure;
semilogy(n, T1, n, T2, n, T3, n, T4)
title('Algorithm Time Complexity (Log Scale)')
xlabel('n'), ylabel('T(n)')
legend('T_1 = n', 'T_2 = n log(n)', 'T_3 = n^2', 'T_4 = 2^n', 'Location', 'best')
알고리즘 성장률 비교할 때는 semilogy 그래프가 훨씬 적절합니다. 일반 그래프로 그리면 T4(n)=2^n 같은 지수 시간이 뒤로 갈수록 혼자 튀어버려서, 나머지 다항 시간 함수들은 그냥 x축 바닥에 붙어있는 선처럼 보이거든요. 로그 스케일을 써야 네 가지 함수가 각각 어느 정도로 커지고 있는지 그 차이와 추세를 제대로 비교할 수 있습니다.
16번
clear; clc;

t = 0:0.1:24;
T = 15 + 10*sin(pi*t/12);

figure;
plot(t, T)
hold on

[max_T, max_idx] = max(T);
[min_T, min_idx] = min(T);

plot(t(max_idx), max_T, 'ro', 'MarkerSize', 8, 'LineWidth', 2)
plot(t(min_idx), min_T, 'bo', 'MarkerSize', 8, 'LineWidth', 2)

title('Temperature over 24 Hours')
xlabel('Time (t)'), ylabel('Temperature (T)')
legend('T(t)', 'Max Temp', 'Min Temp')
hold off
17번
clear; clc;

t = 0:0.1:20;
v = 5*exp(-0.2*t);

subplot(1,2,1)
plot(t, v)
title('Velocity (Linear)')
xlabel('t'), ylabel('v(t)')

subplot(1,2,2)
semilogy(t, v)
title('Velocity (Log Scale)')
xlabel('t'), ylabel('v(t)')
일반 그래프에서는 속도가 0에 가까워지면서 쭉 미끄러지듯 완만하게 줄어드는 곡선으로 보입니다. 그런데 이걸 semilogy로 다시 그리면, 지수 함수로 감소하던 속도 값이 로그 변환되면서 일정한 비율로 떨어지는 직선 형태로 훨씬 직관적이고 깔끔하게 보입니다.
18번
clear; clc;

A = [4, -1, 2; 1, 3, -1; 2, -2, 5];
b = [7; 4; 10];

A_sq = A^2;
A_dot_sq = A.^2;
disp('A^2 (Matrix Multiplication):'), disp(A_sq)
disp('A.^2 (Element-wise Power):'), disp(A_dot_sq)

det_A = det(A);
fprintf('det(A) = %f\n', det_A);
if det_A ~= 0
    disp('행렬식(determinant)이 0이 아니므로 역행렬이 존재합니다.');
else
    disp('행렬식(determinant)이 0이므로 역행렬이 존재하지 않습니다.');
end

x_inv = inv(A)*b;
x_mldiv = A\b;
disp('inv(A)*b의 결과:'), disp(x_inv)
disp('A\b의 결과:'), disp(x_mldiv)

disp('선형 연립방정식 Ax = b의 해:'), disp(x_mldiv)
A^2는 선형대수에서 배운 행렬의 곱셈 법칙대로 A 행렬 두 개를 곱한 결과입니다. 반면에 A.^2처럼 점이 붙으면 행렬 곱셈이 아니라, 그냥 A 행렬 안에 들어있는 원소들을 자기 자리에서 각각 따로따로 제곱만 해준 결과입니다.
19번
clear; clc;

A = [2, -1; 3, 4];
B = [1, 0, -2; 3, 5, 1];
C = [6, -3, 4; 15, 14, -1];

X = A\(C - B);
disp('계산된 행렬 X:'), disp(X)

Check_C = A*X + B;
disp('A*X + B 의 결과 (C와 비교):'), disp(Check_C)
역행렬을 직접 구해서 곱하는 inv(A) 방식은 중간 계산 과정도 불필요하게 복잡해지고 소수점 계산하다가 오차가 생길 확률이 높습니다. 반면에 역슬래시 A\(C-B)를 쓰면 MATLAB 내부에서 가우스 소거법 같은 알고리즘으로 연립방정식을 바로 풀어버리기 때문에 연산 속도도 훨씬 빠르고 계산 결과도 훨씬 정확합니다.
20번
clear; clc;

X = [3 7 2 5; 
     5 9 4 6; 
     8 12 7 10; 
     10 12 9 13; 
     14 18 12 16];

col_means = mean(X);
disp('각 열의 평균:'), disp(col_means)

Xc = X - col_means; 
disp('중심화 행렬 X_c:'), disp(Xc)

S = Xc' * Xc; 
disp('행렬 S (Xc''*Xc):'), disp(S)
행렬 S에서 대각선에 있는 원소들은 자기 자신과의 관계, 즉 각 변수 데이터의 분산을 보여줍니다. 그리고 대각선이 아닌 나머지 비대각 원소들은 서로 다른 두 변수끼리 얼마나 같이 움직이는지, 즉 둘 사이의 상관관계가 어느 정도인지를 나타냅니다.
