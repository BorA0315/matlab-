1번
x = input('input : ');
if x > 0
    y = x^2;
else
    y = 0;
end
fprintf('y = %f', y);
2번
clear; clc;
m = input('input : ');
switch m
    case 1
        disp('January');
    case 2
        disp('February');
    case 3
        disp('March');
    case 4
        disp('April');
    otherwise
        disp('invalid month');
end
3번
clear; clc;
x = [3, -5, 0, 8, -2, 4, -7];
ne_cnt = 0;
for i = 1:length(x)
    if x(i) < 0
        ne_cnt = ne_cnt+ 1;
    end
end
disp(ne_cnt);
4번
clear; clc;
x = [4, -9, 0, 16, -1, 25];
y = zeros(1, length(x));
for i = 1:length(x)
    if x(i) > 0
        y(i) = sqrt(x(i));
    elseif x(i) == 0
        y(i) = 0;
    else
        y(i) = abs(x(i));
    end
end
disp(y);
5번
clear; clc;
a = input('input1 : ');
b = input('input2 : ');
cmd = input('cmd : ', 's');

switch cmd
    case 'add'
        disp(a + b);
    case 'sub'
        disp(a - b);
    case 'mul'
        disp(a * b);
    case 'div'
        if b == 0
            disp('division by zero');
        else
            disp(a / b);
        end
    otherwise
        disp('invalid command');
end
6번
clear; clc;
N = input('input : ');
sum = 0;
for i = 1:N
    if mod(i, 3) == 0 && mod(i, 2) ~= 0
        sum = sum + i;
    end
end
disp(sum)
7번
clear; clc;
N = input('input : ');
a = zeros(1, N);
for k = 1:N
    if mod(k, 2) ~= 0
        a(k) = k^2;
    else
        a(k) = k^3;
    end
end
disp(a);
8번
clear; clc;
price = [19, 18, 22, 21, 25, 19, 17, 21, 27, 29];
ju = 1000;
spent = 0;
earned = 0;
start = ju * price(1);

for p = price
    if p < 20
        ju = ju + 100;
        spent = spent + (100 * p);
    elseif p > 25
        ju = ju - 100;
        earned = earned + (100 * p);
    end
end

final = ju * price(end) + earned - spent;
increase = final - start;

fprintf('(a) 주식을 사는 데 소비한 금액 : $%d\n', spent);
fprintf('(b) 주식을 팔아서 얻어진 금액: $%d\n', earned);
fprintf('(c) 10일째가 지난 후에 보유하고 있는 주식의 수 : %d주\n', ju);
fprintf('(d) 포트폴리오에서 총 가치의 증가분 : $%d', increase);
9번
clear; clc;
connect_type = input('연결 형태 : ', 's');
n = input('저항의 개수 : ');
R = input('각 저항값 [대괄호 안에 작성]: ');

if strcmp(connect_type, '직렬')
    R_eq = sum(R);
    fprintf('직렬연결 등가 저항: %f', R_eq);
elseif strcmp(connect_type, '병렬')
    inv_R_eq = sum(1 ./ R);
    R_eq = 1 / inv_R_eq;
    fprintf('병렬연결 등가 저항: %f', R_eq);
end
