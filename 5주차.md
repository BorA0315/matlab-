x = [-2, -1, 0, 1, 2];

x_sq = squareVec(x);
fprintf('제곱 :');
disp(x_sq);

x_mean = meanVec(x);
fprintf('평균값 :');
disp(x_mean);

x_trans = mixedTransform(x);
fprintf('반환 :');
disp(x_trans);

function y = squareVec(x)
    y = x.^2;
end

function y = meanVec(x)
    y = mean(x);
end

function y = mixedTransform(x)
    y = zeros(size(x));
    for i = 1:length(x)
        if x(i) < 0
            y(i) = abs(x(i));
        elseif x(i) == 0
            y(i) = x(i);
        else
            y(i) = 2 * x(i) + 1;
        end
    end
end


==============

name = input('학생 이름 입력 : ', 's');
scores = input('국어, 영어, 수학 점수 입력 : ');
avg = mean(scores);

fprintf('=== 성적 결과 ===\n');
fprintf('이름 : %s\n', name);
fprintf('과목 점수 : %d %d %d\n', scores(1), scores(2), scores(3));
fprintf('평균 : %.2f\n', avg);

if avg >= 90
    grade = 'Excellent';
elseif avg >= 80
    grade = 'Good';
elseif avg >= 70
    grade = 'Fair';
else
    grade = 'Needs Improvement';
end

fprintf('등급 : %s', grade);


================

applicants = [165 72 1; 172 85 0; 168 91 1; 181 77 1; 174 69 0; 169 88 1]

cond_height = applicants(:, 1) >= 170;
cond_score = applicants(:, 2) >= 80;
cond_interview = applicants(:, 3) == 1;

pass_idx = find(cond_height & cond_score & cond_interview);

passed_data = applicants(pass_idx, :);
final_result = [pass_idx, passed_data];

disp('최종 합격자 정보 (행 번호, 키, 점수, 면접여부):');
disp(final_result);
